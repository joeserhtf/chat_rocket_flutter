name: chat_rocket_flutter
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4
  badges: ^2.0.3
  photo_view: ^0.14.0
  just_audio: ^0.9.27
  stop_watch_timer: ^1.5.0
  cached_network_image: ^3.2.1
  file_picker: ^4.6.1
  intl: ^0.17.0
  positioned_tap_detector_2: ^1.0.4
  url_launcher: ^6.1.4

  dart_meteor_web:
    git:
      url: https://github.com/joeserhtf/dart_meteor
      ref: main
  record: ^4.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4


flutter:

